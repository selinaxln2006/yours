{
  "name": "paa",
  "version": "2.0.0",
  "private": true,
  "type": "module",
  "description": "Personal AI Agent Framework — PAA v2 大脑层（TS 自研核心，参考 DSH(MIT)/Operit(LGPL)/TencentDB(MIT) 设计思想）",
  "scripts": {
    "check": "tsc --noEmit",
    "cli": "node cli/main.ts",
    "test": "node --test --test-timeout=15000"
  },
  "devDependencies": {
    "@types/node": "^24.0.0",
    "typescript": "^5.6.0"
  }
}
